name: Deploy Backend
on:
  push:
    branches:
      - main
    paths:
      - 'backend/**' # Chỉ chạy khi bạn sửa code trong thư mục backend

jobs:
  deploy-backend:
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Lấy code từ GitHub
        uses: actions/checkout@v4

      - name: 🚀 Đẩy code lên InfinityFree (FTP)
        uses: SamKirkland/FTP-Deploy-Action@4.3.3
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          
          # Lấy code từ thư mục này trên GitHub
          local-dir: ./backend/
          
          # Đẩy vào thư mục này trên Host
          server-dir: htdocs/web-be/
          
          # Bỏ qua file rác
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
